fn main() {

    // Variable Scope
    {
        let s = "hello"; // s is valid from this point forward

        // do stuff with s
    }                   // this scop is now over, and s is no longer valid
}
